// var { Post } = require("../models");
const router = require("express").Router();

router.get("/api/activetrip", function (req, res) {
    
    // db.Trips.findAll({}).then(function (dbTrips) {
    //     res.json(dbTrips);
    // });

});

module.exports = router